# Weather-App-react
WeatherApp is a React-based web application built with Vite. It allows users to check real-time weather conditions by entering a city name. The app uses React Hooks (useState, useEffect) for state and side-effect management and fetches data from the OpenWeather API.
